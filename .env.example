SESSION_SECRET=change-me-to-a-long-random-string
# Vercel เท่านั้น (ในเครื่องเว้นว่าง = ใช้ไฟล์ data/*.json และ public/uploads/)
# DATABASE_URL=postgres://...   ← Neon (Vercel Storage) เก็บ collection JSON
# BLOB_READ_WRITE_TOKEN=...     ← Vercel Blob เก็บรูปที่อัปโหลด
